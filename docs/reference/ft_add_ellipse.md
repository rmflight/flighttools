# add data ellipse

Adds an ellipse showing the extent of the data to a plot. Most useful
for PCA type stuff to show whether the samples separate or not.

## Usage

``` r
ft_add_ellipse()
```

## Value

a string
