# create limma code

A helper that provides a template for limma code to programmatically set
up the differences one wants to extract.

## Usage

``` r
create_limma_code()
```

## Value

NULL, invisibly
